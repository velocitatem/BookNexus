on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - name: Compile
          run: cmake -Bbuild -H. && cmake --build build

